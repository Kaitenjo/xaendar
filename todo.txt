Rivedere interpolazioni
- Non funzionano se dopo la ` finale non segue subito la }
`} funziona
`  } non funziona <--- da fixare

Il render generator non aggiunge correttamente il this. alle variabili interne all'interpolazione
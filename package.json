{
  "name": "xendar",
  "version": "0.1.6",
  "author": "Kaitenjo",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "build:schematics": "npm run build --prefix schematics",
    "build:schematics:watch": "npm run build:watch --prefix schematics",
    "build:projects": "npm run build-projects --prefix schematics",
    "check:dependencies": "npm run check-dependencies --prefix schematics",
    "compile:projects": "npm run compile-projects --prefix schematics",
    "deploy": "npm run build:schematics && npm run check:dependencies && npm run update-version && npm run compile:projects && npm run build:projects && npm run pack:projects && npm run publish:projects",
    "i": "npm i --omit-optional --ignore-scripts",
    "install:clean": "rimraf package-lock.json node_modules && npm run i",
    "pack:projects": "npm run pack --prefix schematics",
    "publish:projects": "npm run publish --prefix schematics",
    "start": "nodemon --watch src --watch packages -e ts --exec \"vite build && node --enable-source-maps dist/test.es.js\"",
    "start:debug": "nodemon --watch src --watch packages -e ts --exec \"vite build && node --enable-source-maps --inspect-brk dist/test.es.js\"",
    "update-version": "npm run update-version --prefix schematics"
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.58.7",
    "@types/node": "^25.6.0",
    "depcheck": "^1.4.7",
    "nodemon": "^3.1.14",
    "rimraf": "^6.1.3",
    "type-fest": "^5.6.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vite-plugin-dts": "^4.5.4",
    "vite-tsconfig-paths": "^6.1.1",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "commander": "^14.0.3"
  }
}
